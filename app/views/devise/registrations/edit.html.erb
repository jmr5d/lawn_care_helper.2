<%- model_class = Property -%>

<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for [resource_name, resource.with_properties], :url => registration_path(resource_name), :html => { method: :put } do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :password %> <i>(leave blank if you don't want to change it)</i><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
    <%= f.password_field :current_password, autocomplete: "off" %>
  </div>




<!--   <%= f.fields_for :properties do |p| %>
    <div class="field"><%= p.text_field :address1, :placeholder => "123 Anystreet St", class: "text-field form-control" %></div>
    <div class="field"><%= p.text_field :address2, :placeholder => "Apt 1", class: "text-field form-control" %></div>
    <div class="field"><%= p.text_field :city, :placeholder => "San Francisco", class: "text-field form-control" %></div>
    <div class="field"><%= p.text_field :state, :placeholder => "CA", class: "text-field form-control" %></div>
    <div class="field"><%= p.text_field :zipcode, :placeholder => "90210", class: "text-field form-control" %></div>
    <div class="field"><%= p.text_field :size_in_acres, :placeholder => "Acres, such as .5 or .25", class: "text-field form-control"%></div>
    <%= p.hidden_field :latitude, class: "text-field form-control" %>
    <%= p.hidden_field :longitude, class: "text-field form-control" %>
    <%# p.text_field :altitude, class: "text-field form-control" %>
    <%= p.hidden_field :_destroy %>
    <%= p.check_box :_destroy %>
    <%= p.label :_destroy, "Remove" %>
  <% end %> -->





<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:address1) %></th>
      <th><%= model_class.human_attribute_name(:address2) %></th>
      <th><%= model_class.human_attribute_name(:city) %></th>
      <th><%= model_class.human_attribute_name(:state) %></th>
      <!-- <th><%= model_class.human_attribute_name(:country) %></th> -->
      <th><%= model_class.human_attribute_name(:zipcode) %></th>
      <th><%= model_class.human_attribute_name(:size_in_acres) %></th>
      <th>Remove</th>
      <!-- <th><%=t '.actions', :default => t("helpers.actions") %></th> -->
    </tr>
  </thead>
  <tbody>
    <%= f.fields_for :properties do |property| %>
      <tr>
        <td><%= property.text_field :address1 %></td>
        <td><%= property.text_field :address2, :size => 10 %></td>
        <td><%= property.text_field :city %></td>
        <td><%= property.text_field :state, :size => 3 %></td>
        <!-- <td><%= property.text_field :country %></td> -->
        <td><%= property.text_field :zipcode %></td>
        <td><%= property.text_field :size_in_acres, :size => 5 %></td>
        <td>
    <%= property.hidden_field :_destroy %>
    <%= property.check_box :_destroy %></td>
        <!-- <td>
          <%= link_to property_path(property), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to property_path(property), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td> -->
      </tr>
    <% end %>
  </tbody>
</table>





  <div class="actions">
    <%= f.submit "Update" %>
    <br/><br/>
    <%= link_to "Back", :back %>
  </div>
<% end %>
<br/><br/><br/><br/>

<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>